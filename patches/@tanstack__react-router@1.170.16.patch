diff --git a/dist/cjs/CatchBoundary.cjs b/dist/cjs/CatchBoundary.cjs
index 8ad0d9bfe97b2cfa8a8ebafb85e02dea54bb5866..49010df3bade61f9abdc0e71675f6fc88f949b50 100644
--- a/dist/cjs/CatchBoundary.cjs
+++ b/dist/cjs/CatchBoundary.cjs
@@ -8,6 +8,7 @@ function CatchBoundary(props) {
 	const errorComponent = props.errorComponent ?? ErrorComponent;
 	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(CatchBoundaryImpl, {
 		getResetKey: props.getResetKey,
+		resetKeyStore: props.resetKeyStore,
 		onCatch: props.onCatch,
 		children: ({ error, reset }) => {
 			if (error) return react.createElement(errorComponent, {
@@ -34,6 +35,26 @@ var CatchBoundaryImpl = class extends react.Component {
 	static getDerivedStateFromError(error) {
 		return { error };
 	}
+	componentDidMount() {
+		this.subscribeToResetKey();
+	}
+	componentDidUpdate(previousProps) {
+		if (previousProps.resetKeyStore !== this.props.resetKeyStore) {
+			this.resetKeySubscription?.unsubscribe();
+			this.subscribeToResetKey();
+		}
+	}
+	componentWillUnmount() {
+		this.resetKeySubscription?.unsubscribe();
+	}
+	subscribeToResetKey() {
+		this.resetKeySubscription = this.props.resetKeyStore?.subscribe(() => {
+			if (this.state.error) this.setState({
+				error: null,
+				resetKey: this.props.getResetKey()
+			});
+		});
+	}
 	reset() {
 		this.setState({ error: null });
 	}
diff --git a/dist/cjs/Match.cjs b/dist/cjs/Match.cjs
index d6347522e1d49691a91d5df3869351519ac4ae10..ad8b5968dfeebc3e8b429a2cdeccc924e1899a00 100644
--- a/dist/cjs/Match.cjs
+++ b/dist/cjs/Match.cjs
@@ -17,6 +17,7 @@ let _tanstack_react_store = require("@tanstack/react-store");
 let _tanstack_router_core_isServer = require("@tanstack/router-core/isServer");
 //#region src/Match.tsx
 var matchViewFieldsEqual = (a, b) => a.routeId === b.routeId && a._displayPending === b._displayPending;
+var matchInnerFieldsEqual = (a, b) => a.routeId === b.routeId && a._displayPending === b._displayPending && a._forcePending === b._forcePending && a.status === b.status && a.error === b.error && (0, _tanstack_router_core.deepEqual)(a.loaderDeps, b.loaderDeps) && (0, _tanstack_router_core.deepEqual)(a._strictParams, b._strictParams) && (0, _tanstack_router_core.deepEqual)(a._strictSearch, b._strictSearch);
 var outletMatchSelectionEqual = (a, b) => a[0] === b[0] && a[1] === b[1];
 var Match = react.memo(function MatchImpl({ matchId }) {
 	const router = require_useRouter.useRouter();
@@ -45,12 +46,11 @@ var Match = react.memo(function MatchImpl({ matchId }) {
 		if (process.env.NODE_ENV !== "production") throw new Error(`Invariant failed: Could not find match for matchId "${matchId}". Please file an issue!`);
 		(0, _tanstack_router_core.invariant)();
 	}
-	const resetKey = (0, _tanstack_react_store.useStore)(router.stores.loadedAt, (loadedAt) => loadedAt);
 	const match = (0, _tanstack_react_store.useStore)(matchStore, (value) => value, matchViewFieldsEqual);
 	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(MatchView, {
 		router,
 		matchId,
-		resetKey,
+		resetKey: router.stores.loadedAt.get(),
 		matchState: react.useMemo(() => {
 			const routeId = match.routeId;
 			const parentRouteId = router.routesById[routeId].parentRoute?.id;
@@ -85,6 +85,7 @@ function MatchView({ router, matchId, resetKey, matchState }) {
 			fallback: pendingElement,
 			children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ResolvedCatchBoundary, {
 				getResetKey: () => resetKey,
+				resetKeyStore: router.stores.loadedAt,
 				errorComponent: routeErrorComponent || require_CatchBoundary.ErrorComponent,
 				onCatch: (error, errorInfo) => {
 					if ((0, _tanstack_router_core.isNotFound)(error)) {
@@ -175,7 +176,7 @@ var MatchInner = react.memo(function MatchInnerImpl({ matchId }) {
 		if (process.env.NODE_ENV !== "production") throw new Error(`Invariant failed: Could not find match for matchId "${matchId}". Please file an issue!`);
 		(0, _tanstack_router_core.invariant)();
 	}
-	const match = (0, _tanstack_react_store.useStore)(matchStore, (value) => value);
+	const match = (0, _tanstack_react_store.useStore)(matchStore, (value) => value, matchInnerFieldsEqual);
 	const routeId = match.routeId;
 	const route = router.routesById[routeId];
 	const key = react.useMemo(() => {
diff --git a/dist/cjs/Matches.cjs b/dist/cjs/Matches.cjs
index fb32201c4bbb1574a819feb55f8a6a2440f29817..aed9daaec4df21c9b554ccd32474a844436d15a7 100644
--- a/dist/cjs/Matches.cjs
+++ b/dist/cjs/Matches.cjs
@@ -32,12 +32,13 @@ function MatchesInner() {
 	const router = require_useRouter.useRouter();
 	const _isServer = _tanstack_router_core_isServer.isServer ?? router.isServer;
 	const matchId = _isServer ? router.stores.firstId.get() : (0, _tanstack_react_store.useStore)(router.stores.firstId, (id) => id);
-	const resetKey = _isServer ? router.stores.loadedAt.get() : (0, _tanstack_react_store.useStore)(router.stores.loadedAt, (loadedAt) => loadedAt);
+	const resetKey = router.stores.loadedAt.get();
 	const matchComponent = matchId ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_Match.Match, { matchId }) : null;
 	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_matchContext.matchContext.Provider, {
 		value: matchId,
 		children: router.options.disableGlobalCatchBoundary ? matchComponent : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_CatchBoundary.CatchBoundary, {
 			getResetKey: () => resetKey,
+			resetKeyStore: router.stores.loadedAt,
 			errorComponent: require_CatchBoundary.ErrorComponent,
 			onCatch: process.env.NODE_ENV !== "production" ? (error) => {
 				console.warn(`Warning: The following error wasn't caught by any route! At the very least, consider setting an 'errorComponent' in your RootRoute!`);
diff --git a/dist/cjs/not-found.cjs b/dist/cjs/not-found.cjs
index 0e7bd4321d82143798811370501a489548fbaa40..0c9289fe507876bdb80232f5c2545cb91b3e7cf1 100644
--- a/dist/cjs/not-found.cjs
+++ b/dist/cjs/not-found.cjs
@@ -5,29 +5,14 @@ let _tanstack_router_core = require("@tanstack/router-core");
 let react = require("react");
 react = require_runtime.__toESM(react, 1);
 let react_jsx_runtime = require("react/jsx-runtime");
-let _tanstack_react_store = require("@tanstack/react-store");
 let _tanstack_router_core_isServer = require("@tanstack/router-core/isServer");
 //#region src/not-found.tsx
 function CatchNotFound(props) {
 	const router = require_useRouter.useRouter();
-	if (_tanstack_router_core_isServer.isServer ?? router.isServer) {
-		const resetKey = `not-found-${router.stores.location.get().pathname}-${router.stores.status.get()}`;
-		return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_CatchBoundary.CatchBoundary, {
-			getResetKey: () => resetKey,
-			onCatch: (error, errorInfo) => {
-				if ((0, _tanstack_router_core.isNotFound)(error)) props.onCatch?.(error, errorInfo);
-				else throw error;
-			},
-			errorComponent: ({ error }) => {
-				if ((0, _tanstack_router_core.isNotFound)(error)) return props.fallback?.(error);
-				else throw error;
-			},
-			children: props.children
-		});
-	}
-	const resetKey = `not-found-${(0, _tanstack_react_store.useStore)(router.stores.location, (location) => location.pathname)}-${(0, _tanstack_react_store.useStore)(router.stores.status, (status) => status)}`;
+	const getResetKey = () => `not-found-${router.stores.location.get().pathname}-${router.stores.status.get()}`;
 	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_CatchBoundary.CatchBoundary, {
-		getResetKey: () => resetKey,
+		getResetKey,
+		resetKeyStore: _tanstack_router_core_isServer.isServer ?? router.isServer ? void 0 : router.stores.status,
 		onCatch: (error, errorInfo) => {
 			if ((0, _tanstack_router_core.isNotFound)(error)) props.onCatch?.(error, errorInfo);
 			else throw error;
diff --git a/dist/esm/CatchBoundary.js b/dist/esm/CatchBoundary.js
index 1f47185042beff96ba6c47852df41cb9fd406ea2..f638e31bf24b0721c1b816b0f2de7e7d0118d167 100644
--- a/dist/esm/CatchBoundary.js
+++ b/dist/esm/CatchBoundary.js
@@ -6,6 +6,7 @@ function CatchBoundary(props) {
 	const errorComponent = props.errorComponent ?? ErrorComponent;
 	return /* @__PURE__ */ jsx(CatchBoundaryImpl, {
 		getResetKey: props.getResetKey,
+		resetKeyStore: props.resetKeyStore,
 		onCatch: props.onCatch,
 		children: ({ error, reset }) => {
 			if (error) return React$1.createElement(errorComponent, {
@@ -32,6 +33,26 @@ var CatchBoundaryImpl = class extends React$1.Component {
 	static getDerivedStateFromError(error) {
 		return { error };
 	}
+	componentDidMount() {
+		this.subscribeToResetKey();
+	}
+	componentDidUpdate(previousProps) {
+		if (previousProps.resetKeyStore !== this.props.resetKeyStore) {
+			this.resetKeySubscription?.unsubscribe();
+			this.subscribeToResetKey();
+		}
+	}
+	componentWillUnmount() {
+		this.resetKeySubscription?.unsubscribe();
+	}
+	subscribeToResetKey() {
+		this.resetKeySubscription = this.props.resetKeyStore?.subscribe(() => {
+			if (this.state.error) this.setState({
+				error: null,
+				resetKey: this.props.getResetKey()
+			});
+		});
+	}
 	reset() {
 		this.setState({ error: null });
 	}
diff --git a/dist/esm/Match.js b/dist/esm/Match.js
index 8da8d0f108b2dcd57fc6c9a853317d164e31776b..5703e63b4c4382c25e54f01bbb625bd0bc734fe7 100644
--- a/dist/esm/Match.js
+++ b/dist/esm/Match.js
@@ -8,13 +8,14 @@ import { CatchNotFound } from "./not-found.js";
 import { SafeFragment } from "./SafeFragment.js";
 import { renderRouteNotFound } from "./renderRouteNotFound.js";
 import { ScrollRestoration } from "./scroll-restoration.js";
-import { createControlledPromise, getLocationChangeInfo, invariant, isNotFound, isRedirect, rootRouteId } from "@tanstack/router-core";
+import { createControlledPromise, deepEqual, getLocationChangeInfo, invariant, isNotFound, isRedirect, rootRouteId } from "@tanstack/router-core";
 import * as React$1 from "react";
 import { Fragment, jsx, jsxs } from "react/jsx-runtime";
 import { useStore } from "@tanstack/react-store";
 import { isServer } from "@tanstack/router-core/isServer";
 //#region src/Match.tsx
 var matchViewFieldsEqual = (a, b) => a.routeId === b.routeId && a._displayPending === b._displayPending;
+var matchInnerFieldsEqual = (a, b) => a.routeId === b.routeId && a._displayPending === b._displayPending && a._forcePending === b._forcePending && a.status === b.status && a.error === b.error && deepEqual(a.loaderDeps, b.loaderDeps) && deepEqual(a._strictParams, b._strictParams) && deepEqual(a._strictSearch, b._strictSearch);
 var outletMatchSelectionEqual = (a, b) => a[0] === b[0] && a[1] === b[1];
 var Match = React$1.memo(function MatchImpl({ matchId }) {
 	const router = useRouter();
@@ -43,12 +44,11 @@ var Match = React$1.memo(function MatchImpl({ matchId }) {
 		if (process.env.NODE_ENV !== "production") throw new Error(`Invariant failed: Could not find match for matchId "${matchId}". Please file an issue!`);
 		invariant();
 	}
-	const resetKey = useStore(router.stores.loadedAt, (loadedAt) => loadedAt);
 	const match = useStore(matchStore, (value) => value, matchViewFieldsEqual);
 	return /* @__PURE__ */ jsx(MatchView, {
 		router,
 		matchId,
-		resetKey,
+		resetKey: router.stores.loadedAt.get(),
 		matchState: React$1.useMemo(() => {
 			const routeId = match.routeId;
 			const parentRouteId = router.routesById[routeId].parentRoute?.id;
@@ -83,6 +83,7 @@ function MatchView({ router, matchId, resetKey, matchState }) {
 			fallback: pendingElement,
 			children: /* @__PURE__ */ jsx(ResolvedCatchBoundary, {
 				getResetKey: () => resetKey,
+				resetKeyStore: router.stores.loadedAt,
 				errorComponent: routeErrorComponent || ErrorComponent,
 				onCatch: (error, errorInfo) => {
 					if (isNotFound(error)) {
@@ -173,7 +174,7 @@ var MatchInner = React$1.memo(function MatchInnerImpl({ matchId }) {
 		if (process.env.NODE_ENV !== "production") throw new Error(`Invariant failed: Could not find match for matchId "${matchId}". Please file an issue!`);
 		invariant();
 	}
-	const match = useStore(matchStore, (value) => value);
+	const match = useStore(matchStore, (value) => value, matchInnerFieldsEqual);
 	const routeId = match.routeId;
 	const route = router.routesById[routeId];
 	const key = React$1.useMemo(() => {
diff --git a/dist/esm/Matches.js b/dist/esm/Matches.js
index 1427162bce0c91672b40e13be9bcabd29145b1a6..c2c34faca1fb5c37b76be68bd1b5ee4be137f6e1 100644
--- a/dist/esm/Matches.js
+++ b/dist/esm/Matches.js
@@ -30,12 +30,13 @@ function MatchesInner() {
 	const router = useRouter();
 	const _isServer = isServer ?? router.isServer;
 	const matchId = _isServer ? router.stores.firstId.get() : useStore(router.stores.firstId, (id) => id);
-	const resetKey = _isServer ? router.stores.loadedAt.get() : useStore(router.stores.loadedAt, (loadedAt) => loadedAt);
+	const resetKey = router.stores.loadedAt.get();
 	const matchComponent = matchId ? /* @__PURE__ */ jsx(Match, { matchId }) : null;
 	return /* @__PURE__ */ jsx(matchContext.Provider, {
 		value: matchId,
 		children: router.options.disableGlobalCatchBoundary ? matchComponent : /* @__PURE__ */ jsx(CatchBoundary, {
 			getResetKey: () => resetKey,
+			resetKeyStore: router.stores.loadedAt,
 			errorComponent: ErrorComponent,
 			onCatch: process.env.NODE_ENV !== "production" ? (error) => {
 				console.warn(`Warning: The following error wasn't caught by any route! At the very least, consider setting an 'errorComponent' in your RootRoute!`);
diff --git a/dist/esm/not-found.js b/dist/esm/not-found.js
index c4e952515a665e79f2762c4e57c61a50e6cbfc23..fb8d312e8ed81256273e745a80039dd174b2751c 100644
--- a/dist/esm/not-found.js
+++ b/dist/esm/not-found.js
@@ -3,29 +3,14 @@ import { useRouter } from "./useRouter.js";
 import { isNotFound } from "@tanstack/router-core";
 import "react";
 import { jsx } from "react/jsx-runtime";
-import { useStore } from "@tanstack/react-store";
 import { isServer } from "@tanstack/router-core/isServer";
 //#region src/not-found.tsx
 function CatchNotFound(props) {
 	const router = useRouter();
-	if (isServer ?? router.isServer) {
-		const resetKey = `not-found-${router.stores.location.get().pathname}-${router.stores.status.get()}`;
-		return /* @__PURE__ */ jsx(CatchBoundary, {
-			getResetKey: () => resetKey,
-			onCatch: (error, errorInfo) => {
-				if (isNotFound(error)) props.onCatch?.(error, errorInfo);
-				else throw error;
-			},
-			errorComponent: ({ error }) => {
-				if (isNotFound(error)) return props.fallback?.(error);
-				else throw error;
-			},
-			children: props.children
-		});
-	}
-	const resetKey = `not-found-${useStore(router.stores.location, (location) => location.pathname)}-${useStore(router.stores.status, (status) => status)}`;
+	const getResetKey = () => `not-found-${router.stores.location.get().pathname}-${router.stores.status.get()}`;
 	return /* @__PURE__ */ jsx(CatchBoundary, {
-		getResetKey: () => resetKey,
+		getResetKey,
+		resetKeyStore: isServer ?? router.isServer ? void 0 : router.stores.status,
 		onCatch: (error, errorInfo) => {
 			if (isNotFound(error)) props.onCatch?.(error, errorInfo);
 			else throw error;
diff --git a/src/CatchBoundary.tsx b/src/CatchBoundary.tsx
index 98743f944779e1ab5565d7fac3ca50761cb9b5ec..cf18eb201107611217f140d5726c6f4ba1455343 100644
--- a/src/CatchBoundary.tsx
+++ b/src/CatchBoundary.tsx
@@ -4,8 +4,13 @@ import * as React from 'react'
 import type { ErrorRouteComponent } from './route'
 import type { ErrorInfo } from 'react'
 
+type ResetKeyStore = {
+  subscribe: (listener: () => void) => { unsubscribe: () => void }
+}
+
 export function CatchBoundary(props: {
   getResetKey: () => number | string
+  resetKeyStore?: ResetKeyStore
   children: React.ReactNode
   errorComponent?: ErrorRouteComponent
   onCatch?: (error: Error, errorInfo: ErrorInfo) => void
@@ -15,6 +20,7 @@ export function CatchBoundary(props: {
   return (
     <CatchBoundaryImpl
       getResetKey={props.getResetKey}
+      resetKeyStore={props.resetKeyStore}
       onCatch={props.onCatch}
       children={({ error, reset }) => {
         if (error) {
@@ -32,6 +38,7 @@ export function CatchBoundary(props: {
 
 class CatchBoundaryImpl extends React.Component<{
   getResetKey: () => number | string
+  resetKeyStore?: ResetKeyStore
   children: (props: {
     error: Error | null
     reset: () => void
@@ -39,6 +46,7 @@ class CatchBoundaryImpl extends React.Component<{
   onCatch?: (error: Error, errorInfo: ErrorInfo) => void
 }> {
   state = { error: null } as { error: Error | null; resetKey?: string | number }
+  resetKeySubscription?: { unsubscribe: () => void }
 
   static getDerivedStateFromProps(
     props: { getResetKey: () => string | number },
@@ -55,6 +63,28 @@ class CatchBoundaryImpl extends React.Component<{
   static getDerivedStateFromError(error: Error) {
     return { error }
   }
+  componentDidMount() {
+    this.subscribeToResetKey()
+  }
+  componentDidUpdate(previousProps: Readonly<typeof this.props>) {
+    if (previousProps.resetKeyStore !== this.props.resetKeyStore) {
+      this.resetKeySubscription?.unsubscribe()
+      this.subscribeToResetKey()
+    }
+  }
+  componentWillUnmount() {
+    this.resetKeySubscription?.unsubscribe()
+  }
+  subscribeToResetKey() {
+    this.resetKeySubscription = this.props.resetKeyStore?.subscribe(() => {
+      if (this.state.error) {
+        this.setState({
+          error: null,
+          resetKey: this.props.getResetKey(),
+        })
+      }
+    })
+  }
   reset() {
     this.setState({ error: null })
   }
diff --git a/src/Match.tsx b/src/Match.tsx
index 5de5546aeb8fd6f66e41b17744c7e0ec48f19b16..9707739572508fd6b95205cb8b60ddde6c9f0437 100644
--- a/src/Match.tsx
+++ b/src/Match.tsx
@@ -4,6 +4,7 @@ import * as React from 'react'
 import { useStore } from '@tanstack/react-store'
 import {
   createControlledPromise,
+  deepEqual,
   getLocationChangeInfo,
   invariant,
   isNotFound,
@@ -35,6 +36,16 @@ type OutletMatchSelection = [
 const matchViewFieldsEqual = (a: AnyRouteMatch, b: AnyRouteMatch) =>
   a.routeId === b.routeId && a._displayPending === b._displayPending
 
+const matchInnerFieldsEqual = (a: AnyRouteMatch, b: AnyRouteMatch) =>
+  a.routeId === b.routeId &&
+  a._displayPending === b._displayPending &&
+  a._forcePending === b._forcePending &&
+  a.status === b.status &&
+  a.error === b.error &&
+  deepEqual(a.loaderDeps, b.loaderDeps) &&
+  deepEqual(a._strictParams, b._strictParams) &&
+  deepEqual(a._strictSearch, b._strictSearch)
+
 const outletMatchSelectionEqual = (
   a: OutletMatchSelection,
   b: OutletMatchSelection,
@@ -93,8 +104,6 @@ export const Match = React.memo(function MatchImpl({
     invariant()
   }
   // eslint-disable-next-line react-hooks/rules-of-hooks
-  const resetKey = useStore(router.stores.loadedAt, (loadedAt) => loadedAt)
-  // eslint-disable-next-line react-hooks/rules-of-hooks
   const match = useStore(matchStore, (value) => value, matchViewFieldsEqual)
   // eslint-disable-next-line react-hooks/rules-of-hooks
   const matchState = React.useMemo(() => {
@@ -114,7 +123,7 @@ export const Match = React.memo(function MatchImpl({
     <MatchView
       router={router}
       matchId={matchId}
-      resetKey={resetKey}
+      resetKey={router.stores.loadedAt.get()}
       matchState={matchState}
     />
   )
@@ -184,6 +193,7 @@ function MatchView({
         <ResolvedSuspenseBoundary fallback={pendingElement}>
           <ResolvedCatchBoundary
             getResetKey={() => resetKey}
+            resetKeyStore={router.stores.loadedAt}
             errorComponent={routeErrorComponent || ErrorComponent}
             onCatch={(error, errorInfo) => {
               // Forward not found errors (we don't want to show the error component for these)
@@ -401,7 +411,7 @@ export const MatchInner = React.memo(function MatchInnerImpl({
     invariant()
   }
   // eslint-disable-next-line react-hooks/rules-of-hooks
-  const match = useStore(matchStore, (value) => value)
+  const match = useStore(matchStore, (value) => value, matchInnerFieldsEqual)
   const routeId = match.routeId as string
   const route = router.routesById[routeId] as AnyRoute
   // eslint-disable-next-line react-hooks/rules-of-hooks
diff --git a/src/Matches.tsx b/src/Matches.tsx
index 62d0fc42ae6ccba3e584794cabc44595d1031f4c..808436c1b8bf1f0d385b30ae54e24ced005570f6 100644
--- a/src/Matches.tsx
+++ b/src/Matches.tsx
@@ -81,10 +81,7 @@ function MatchesInner() {
     ? router.stores.firstId.get()
     : // eslint-disable-next-line react-hooks/rules-of-hooks
       useStore(router.stores.firstId, (id) => id)
-  const resetKey = _isServer
-    ? router.stores.loadedAt.get()
-    : // eslint-disable-next-line react-hooks/rules-of-hooks
-      useStore(router.stores.loadedAt, (loadedAt) => loadedAt)
+  const resetKey = router.stores.loadedAt.get()
 
   const matchComponent = matchId ? <Match matchId={matchId} /> : null
 
@@ -95,6 +92,7 @@ function MatchesInner() {
       ) : (
         <CatchBoundary
           getResetKey={() => resetKey}
+          resetKeyStore={router.stores.loadedAt}
           errorComponent={ErrorComponent}
           onCatch={
             process.env.NODE_ENV !== 'production'
diff --git a/src/not-found.tsx b/src/not-found.tsx
index ce3a04c74860a764a39a7469bbc307785b48dfaa..bb72cb245387949a56ba9992a96525d0f50f5e73 100644
--- a/src/not-found.tsx
+++ b/src/not-found.tsx
@@ -1,7 +1,6 @@
 import * as React from 'react'
 import { isNotFound } from '@tanstack/router-core'
 import { isServer } from '@tanstack/router-core/isServer'
-import { useStore } from '@tanstack/react-store'
 import { CatchBoundary } from './CatchBoundary'
 import { useRouter } from './useRouter'
 import type { ErrorInfo } from 'react'
@@ -13,48 +12,15 @@ export function CatchNotFound(props: {
   children: React.ReactNode
 }) {
   const router = useRouter()
-
-  if (isServer ?? router.isServer) {
-    const pathname = router.stores.location.get().pathname
-    const status = router.stores.status.get()
-    const resetKey = `not-found-${pathname}-${status}`
-
-    return (
-      <CatchBoundary
-        getResetKey={() => resetKey}
-        onCatch={(error, errorInfo) => {
-          if (isNotFound(error)) {
-            props.onCatch?.(error, errorInfo)
-          } else {
-            throw error
-          }
-        }}
-        errorComponent={({ error }) => {
-          if (isNotFound(error)) {
-            return props.fallback?.(error)
-          } else {
-            throw error
-          }
-        }}
-      >
-        {props.children}
-      </CatchBoundary>
-    )
-  }
-
-  // TODO: Some way for the user to programmatically reset the not-found boundary?
-  // eslint-disable-next-line react-hooks/rules-of-hooks -- condition is static
-  const pathname = useStore(
-    router.stores.location,
-    (location) => location.pathname,
-  )
-  // eslint-disable-next-line react-hooks/rules-of-hooks -- condition is static
-  const status = useStore(router.stores.status, (status) => status)
-  const resetKey = `not-found-${pathname}-${status}`
+  const getResetKey = () =>
+    `not-found-${router.stores.location.get().pathname}-${router.stores.status.get()}`
 
   return (
     <CatchBoundary
-      getResetKey={() => resetKey}
+      getResetKey={getResetKey}
+      resetKeyStore={
+        isServer ?? router.isServer ? undefined : router.stores.status
+      }
       onCatch={(error, errorInfo) => {
         if (isNotFound(error)) {
           props.onCatch?.(error, errorInfo)
